version: '3.8'
services:
  backend:
    build: 
      context: ./{BACKEND_NAME}
      dockerfile: Dockerfile.development
    command: go run .
    volumes:
      - ./{BACKEND_NAME}/:/{PACKAGE_NAME}/backend/
    ports:
      - "8000:8000"
    env_file:
      - .env.development
